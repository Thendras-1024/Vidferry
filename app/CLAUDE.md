# app 模块约束

- 后端业务按 `api`、`core`、`db`、`cli` 和 `utils` 的职责放置，避免跨层复制逻辑。
- 公共数据字段和接口改动保持向后兼容；涉及数据库 schema 时同步 PostgreSQL 迁移文件。
- 不在后端日志、异常响应或调试输出中泄露密钥、Cookie、密码和完整本地凭据路径。
- 未知异常不要改写成粗略编号；只有根因明确时才按现有错误规范包装，并保留 `__cause__` 和 traceback。
