# Vidferry runtime dependencies.
#
# This file is the recommended Conda + pip install entry for Windows users:
#
#   conda create -n vidferry python=3.12 -y
#   conda activate vidferry
#   pip install -r requirements.txt
#   pip install -e .
#
# Keep this file limited to project runtime dependencies. Do not add local
# developer tools, Codex plugins, editable Git dependencies, or environment
# snapshots generated by `pip freeze`.

# Web backend
Flask[async]==3.1.1
flask-cors==6.0.0
argon2-cffi==25.1.0
psycopg[binary,pool]>=3.2,<4

# Core HTTP/logging/config
requests==2.32.3
loguru==0.7.3

# Video download, ASR, translation, and media processing
yt-dlp>=2025.12.8
faster-whisper==1.2.1
ctranslate2==4.8.1
deep-translator>=1.11.4
imageio-ffmpeg>=0.5.1
opencv-python>=4.13.0.92

# Browser automation for platform publishing
patchright==1.58.2
playwright>=1.53.0

# Agent orchestration
langgraph==1.2.8

# Feishu long-connection robot
lark-oapi==1.7.1

# QR code and platform helper libraries
qrcode==8.2
segno>=1.6.6
xhs>=0.2.13

# Windows 下 B 站扫码登录的伪终端支持
pywinpty==3.0.5; sys_platform == "win32"
